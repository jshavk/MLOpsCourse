name: duration-prediction
channels:
  - defaults
  - conda-forge
dependencies:
  # Python version
  - python=3.9.6
  
  # Core data science packages
  - pandas>=1.3.0,<2.0.0
  - numpy>=1.21.0,<2.0.0
  - scikit-learn>=0.24.0,<2.0.0
  
  
  # Visualization
  - matplotlib>=3.3.0
  - seaborn>=0.11.0
  
  # Utilities
  - pip>=21.0.0
  - setuptools>=50.0.0
  
  # Pip-managed packages (if any)
  - pip
  
  # Development tools (optional)
  - black>=21.0
  - pylint>=2.0.0
  - pytest>=6.0.0